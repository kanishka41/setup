import 'package:flutter/material.dart';

class  Wemoms extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Your first page UI goes here
    return Scaffold(
      appBar: AppBar(
        title: Text(' '),
      ),
      body: Center(
        child: Text('This is the  ThirdPage'),
      ),
    );
  }
}
