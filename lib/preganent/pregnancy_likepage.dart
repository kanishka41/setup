import 'package:flutter/material.dart';

class LikePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Your first page UI goes here
    return Scaffold(
      appBar: AppBar(
        title: Text(' LikePage'),
      ),
      body: Center(
        child: Text('This is the LikePage'),
      ),
    );
  }
}
